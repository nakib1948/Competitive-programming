#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define rep(i, n) for (i = 0; i < n; i++)
#define pb push_back
#define mp make_pair
#define ff first
#define ss second

int main()
{
    ll t;
    cin >> t;
    while (t--)
    {
        ll i, j, x, y, z, count = 0, sum = 0, n;
        string s;
        cin >> s;
        for (i = s.size() - 1; i >= 0; i--)
        {
            cout << s[i] << " ";
        }
        cout << endl;
    }

    return 0;
}
